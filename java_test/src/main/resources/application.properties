# Application Configuration
# Path to the person.json file (relative to project root)
person.json.path=../data/person.json

# Path where signature files will be saved
signature.output.path=../output/signature.sig.b64

# Certificate thumbprint for TPM operations
cert.thumbprint=472F5B392D52BB109345DA5BD6649E3AE0AE91E0

# Webhook URL for testing HTTP requests
webhook.url=http://localhost:8080/
# Server Configuration
server.port=8080
server.host=localhost
